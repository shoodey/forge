pre-commit:
  commands:
    check:
      run: pnpm check

commit-msg:
  commands:
    commitlint:
      run: pnpm --dir ./tools/commit exec commitlint --config ./config/commitlint.config.mjs --edit {1}
